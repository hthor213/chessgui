# mentor via gpt

- when: 2026-07-15T12:32:41-07:00
- model: gpt-5.3-chat-latest
- inputs: ask=915ch, intent=2072ch, files=24

---

Alright, this is a strong system already — the interesting work now is tightening the seams so the next 4–6 weeks don’t sprawl. I’ll stick tightly to your four asks.

---

# (a) MISSING — what you will regret not specifying

1. **Persona definition contract (214/218 gap)**
   You describe personas conceptually, but there is no *fully specified, versioned schema* that the runner, arena, and UI all agree on.
   - What exactly is persisted? (book format, priors, sampling params, backend type)
   - How are versions migrated when priors change?
   - How do you guarantee reproducibility of a persona match?
   Have you considered freezing personas as immutable snapshots (like model checkpoints) so exhibitions are reproducible?

2. **Move selection algorithm for personas (critical)**
   You say “Maia + book + priors,” but not:
   - Sampling temperature / top-p / entropy floor
   - Determinism vs stochasticity
   - When to override policy with search (verification)
   This is *the* fidelity lever. Without writing it down, you’ll get drift and non-debuggable behavior.

3. **Opening book format + merge rules**
   Especially for dad:
   - How do you weight moves? Frequency? Recency? opponent strength?
   - How do you merge chess.com + future arena data?
   - How do you decay old data?
   This becomes your silent bias engine — worth specifying now.

4. **Game persistence model (local vs web)**
   You have:
   - local SQLite (app)
   - server DB (arena)
   But no spec for:
   - shared schema?
   - sync path?
   - what counts as canonical?
   This will bite when you want cross-surface analysis.

5. **Clock + time model for personas**
   You’ve got 216 (great), but:
   - personas (policy nets) are “time invariant” — but humans aren’t
   - do personas simulate time pressure mistakes?
   - how does clock affect move quality distribution?
   This matters for realism.

6. **Evaluation ground truth for persona tuning**
   You collect “felt like him” — good.
   Missing:
   - how do you *quantify* improvement?
   - what metric decides a change is accepted?
   (Move-match? ACPL? error profile similarity?)

7. **Arena backend scaling assumptions**
   Even if small:
   - concurrent games?
   - per-move latency budget?
   - engine pooling vs spawn-per-game?
   This affects architecture now, not later.

8. **Security / sandboxing for engine execution (web)**
   You’re running engines server-side:
   - process isolation?
   - resource limits?
   - crash handling?
   This is easy to ignore until it explodes.

---

# (b) IMPROVE — per-spec sharpening

## 214 Persona Simulator

1. **Done criteria lack falsifiability**
   “feels like him” is captured, but:
   - what % move-match improvement = success?
   - what baseline?
   Suggest: define acceptance like  
   “+8% move-match@1 over Maia baseline on held-out set”

2. **Tier 3 (priors) is under-specified**
   You say “only priors that improve move-match survive”
   → how are they injected?
   - bias policy distribution?
   - post-sample filtering?
   - value adjustment?

3. **Dad data sparsity risk**
   You acknowledge it, but haven’t priced:
   - overfitting to 45 games
   - book over-dominance
   Have you considered:
   - clustering dad vs population → hybrid prior?

---

## 218 Bot Roster

1. **Participant abstraction is strong, but missing lifecycle**
   - how are bots added/removed?
   - how are weights/assets fetched/validated?
   - how do you handle missing dependencies?

2. **Persona arm in runner — biggest risk not priced**
   This is non-trivial:
   - async policy calls
   - stochastic moves
   - stateful priors
   Have you considered defining a strict per-move API like:
   `fn select_move(position, context) -> Move + metadata`

3. **Avatars pipeline**
   You decided caricatures, but:
   - generation pipeline?
   - caching?
   - attribution/licensing for source photos?

---

## 217 Persona Arena

1. **Auth + privacy is under-scoped**
   You intentionally avoided ToS, but:
   - what data is stored?
   - can users delete games?
   - retention policy?
   Even for family, write it down.

2. **Latency UX not defined**
   - acceptable move delay?
   - thinking indicator?
   - fallback if engine stalls?

3. **Failure modes**
   - engine crash mid-game
   - network drop
   - partial PGN
   No recovery spec yet.

---

## 210 Tournament

1. **Engine lifecycle leaks risk**
   You mention cleanup, but:
   - max processes?
   - watchdog?
   - crash restart?

2. **Eval tagging depth not justified**
   Why depth 12?
   What error margin is acceptable?

3. **Probability map statistical validity**
   No mention of:
   - confidence intervals
   - minimum samples per bucket

---

## 213 Elo-conditioned evaluator

1. **Tier 0 blend formula is underspecified**
   This is core and needs:
   - exact formula
   - calibration constants
   - failure cases (mates, extreme evals)

2. **Phase detection heuristic is fragile**
   You use simple rules — have you tested stability across transitions?

---

## 215 Training program

1. **Metrics pipeline fragile**
   You rely on:
   - manual triggers
   - external scripts
   This will drift. Worth defining:
   - single source of truth for metrics

2. **Exit criteria not always measurable yet**
   Some depend on future specs → risk of blocked progression.

---

# (c) CONSISTENCY — contradictions / drift

1. **Spec status drift**
   - 016 marked implemented
   - README still says draft  
   → your dependency graph is stale and misleading.

2. **Duplicate spec numbers**
   - 001 appears twice (board + setup)
   This will break tooling later.

3. **Engine spec duplication**
   - 011 appears twice (analysis vs wire-up)
   Some “done” items conflict.

4. **Persona ownership split**
   - 214 defines persona
   - 218 defines runtime behavior
   - 217 consumes
   But:
   - move selection logic is nowhere canonical  
   → ownership unclear.

5. **Eval storage inconsistency**
   - 202: eval in node
   - 210: evaluator produces eval stream
   - 212: consumes evals
   Missing: single canonical eval format.

6. **Naming drift**
   - “rival”, “dad”, “persona”, “bot”
   These are overlapping but not cleanly separated concepts.

7. **Platform stance vs Tauri assumptions**
   - Vision: platform-agnostic
   - Many specs still assume Tauri IPC  
   → web path not fully decoupled yet.

---

# (d) PROJECT PLAN — what to build next (critical path)

You already have a lot running. The goal is **Christmas = beat dad**, not “complete platform.”

## Phase 1 (next 2–3 weeks): stabilize the loop

**Goal: make sparring vs dad maximally realistic + usable daily**

1. Lock persona core (CRITICAL PATH)
   - Define persona config schema (214)
   - Implement deterministic move selection pipeline
   - Add logging per move (policy probs, chosen move)

2. Improve dad simulator fidelity
   - Opening book weighting (frequency + recency)
   - Add light verification search (1–2 plies SF)
   - Prevent obvious blunders Maia makes that dad wouldn’t

3. Close feedback loop
   - Store realism feedback structured
   - Build quick review UI: “show last 3 weird moves”

4. Fix spar UX gaps
   - move numbers (218)
   - back/forward review (218)
   - stable clocks

---

## Phase 2 (weeks 3–5): make it addictive for dad (Arena MVP)

**Goal: dad actually plays repeatedly**

1. Ship Arena Tier 0
   - Google auth
   - 3 bots (Gudmundur, Fischer, Kasparov)
   - stable game persistence

2. Ensure low friction
   - instant game start (<1s)
   - visible thinking but not slow

3. Add “history” view
   - last games
   - quick replay

4. Instrument everything
   - moves, time, outcomes
   - this feeds persona tuning

---

## Phase 3 (weeks 5–7): training loop for you

1. Integrate:
   - spar results → training metrics (215)
   - mistake taxonomy (213/212)

2. Add:
   - opening leak detection vs dad
   - endgame conversion tracking

3. Light automation:
   - weekly metric update
   - simple trajectory view

---

## Phase 4 (final stretch): polish + edge

1. Persona tuning passes (based on feedback)
2. Stability + bug fixing
3. Practice conditions match Florida (time control, environment)

---

# SPECIAL: Making the Persona Simulator MUCH better

This is the most leverage in the entire project.

## 1. Move selection = policy + verification (hybrid)

Right now:
- Maia policy → move

Upgrade to:

- Step 1: get Maia policy (top-k or top-p)
- Step 2: run **cheap Stockfish verification**
  - evaluate top 3–5 moves
- Step 3: reweight:
  - final_score = policy_prob^α * exp(-eval_penalty)

This:
- keeps human move distribution
- avoids absurd blunders

Have you considered tuning α per persona?

---

## 2. Temperature schedule (human variability)

Humans are not constant temperature.

Implement:
- early game: low temperature (book-like)
- middlegame: higher
- time pressure: spike temperature

This alone increases realism massively.

---

## 3. Error model from corpus (huge win)

You have 11M games.

Instead of random mistakes:
- learn:
  - probability of blunder given eval + phase + time
- inject mistakes ONLY when:
  - human band would

This avoids “engine weirdness” and creates **human-like failure timing**.

---

## 4. Opening → middlegame transition handling

Right now: book → Maia

Improve:
- keep a “style bias” for N moves after leaving book
- e.g.:
  - aggressive players keep attacking moves overweighted

---

## 5. Opponent-aware play (important for dad)

Dad doesn’t play in a vacuum.

Add:
- opponent model (you)
- slight adaptation:
  - exploit known weaknesses
  - avoid lines you punish well

Even a tiny bias here makes it feel *personal*.

---

## 6. Sequence-level coherence (big missing piece)

Humans don’t choose moves independently.

Add:
- short plan memory (2–4 moves)
- bias toward consistency:
  - if attacking kingside, continue attack

Without this, bots feel “randomly smart.”

---

## 7. Endgame specialization

Maia is weakest here.

Add:
- fallback:
  - if low material → switch to:
    - tablebase (if trivial)
    - or deeper SF but filtered

---

## 8. Move-match training loop (offline improvement)

Use your DB:

1. take persona games
2. predict moves
3. compare to real
4. optimize:
   - α (policy vs eval)
   - temperature
   - priors

This becomes your **automatic tuning system**

---

## 9. Style priors that actually matter

Only keep priors that measurably improve:

- piece preference (knights vs bishops)
- trade tendency
- risk tolerance (eval variance)

Drop vague ones.

---

## 10. Human-likeness metrics (define them!)

Right now implicit. Make explicit:

- move-match@k
- ACPL difference vs real player
- error timing similarity
- opening distribution KL-divergence

These become your objective function.

---

## Questions to push your thinking

1. What exact function picks a move in a persona today? Could you write it in 20 lines?
2. If two runs of the same game differ, is that intentional or a bug?
3. What’s the single metric that tells you “this feels more like dad than yesterday”?
4. Where do you log *why* a move was chosen? (critical for debugging realism)

---

### Unread specs that would have helped

- spec:217 — only high-level; backend/API details missing for stronger infra critique
- spec:218 — lacks concrete persona execution API details
- spec:214 — missing exact move-selection algorithm details (most critical gap)
