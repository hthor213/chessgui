{
  "name": "chessgui",
  "private": true,
  "version": "0.3.0",
  "license": "GPL-3.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -C apps/desktop dev",
    "dev:web": "pnpm -C apps/web dev",
    "build": "pnpm -C apps/desktop build",
    "build:web": "pnpm -C apps/web build",
    "tauri": "pnpm -C apps/desktop tauri",
    "tsc": "tsc -p apps/desktop && tsc -p apps/web",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.2"
  }
}
