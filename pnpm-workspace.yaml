# spec:220 step 4 — workspace scaffolding, zero moves.
# The repo root is the sole workspace package for now; steps 5-7 add
# packages/* (core, ui) and apps/* (desktop, web) as they are extracted.
packages:
  - .
